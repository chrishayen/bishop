/// A person with a name and age.
/// This is a multi-line doc comment.
Person :: struct {
    /// The person's full name
    name str,
    /// Their current age in years
    age int
}

/// Creates a greeting message for the person.
/// Returns a friendly greeting string.
Person :: greet(self, str greeting) -> str {
    return greeting;
}

/// Checks if the person is an adult.
/// Returns true if age >= 18.
Person :: is_adult(self) -> bool {
    if self.age >= 18 {
        return true;
    }

    return false;
}

/// Adds two integers together.
///
/// # Example
/// ```
/// result := add(2, 3);
/// ```
fn add(int a, int b) -> int {
    return a + b;
}

/// A simple test function (should be skipped in docs).
fn test_add() {
    assert_eq(add(2, 3), 5);
}

/// Regular comments are ignored:
// This is a regular comment, not a doc comment
fn multiply(int a, int b) -> int {
    return a * b;
}
